<?xml version="1.0" encoding="UTF-8"?>
<sqlMap namespace="MidSerAddr">
	<sql id="SelectMidSerAddrList">
		 <![CDATA[
		    SELECT a.ID,
		       a.COMPANY_ID,
		       a.LINK,
		       COMPANY_NAME
			FROM   MIDDLE_SER_ADDR a
			WHERE  ( 1 = 1 )
			<#if id?exists && id!="">
			  And a.ID=:id
			</#if>
			<#if companyIdList?exists && companyIdList!="">
			  And a.COMPANY_ID in (${companyIdList})
			</#if>
			<#if link?exists && link!="">
			  And a.LINK=:link
			</#if>
			<#if companyName?exists && companyName!="">
			  And a.COMPANY_NAME=:companyName
			</#if>
			<#if oderbyField?exists && oderbyField!="">
			  Order by ${oderbyField}
			</#if>
			<#if oderbyAscOrDesc?exists && oderbyAscOrDesc!="">
			  ${oderbyAscOrDesc}	
			</#if>
		]]>
	</sql>
	<sql id="DeleteMidSerAddr" >
		<![CDATA[
			DELETE FROM MIDDLE_SER_ADDR
			WHERE  ID = :id
		]]>
	</sql>
	<sql id="AddMidSerAddr">
		<![CDATA[
			INSERT INTO MIDDLE_SER_ADDR
			            (COMPANY_ID,
		       			LINK,
		       			COMPANY_NAME)
			VALUES      ( :companyId,
			              :link,
			              :companyName) 
         ]]>
	</sql>
	<sql id="UpdateMidSerAddr">
		<![CDATA[
			UPDATE MIDDLE_SER_ADDR
			SET    
			<#if companyId?exists && companyId!="">
					COMPANY_ID = :companyId,
			</#if>
			<#if link?exists && link!="">
			        LINK = :link,
			</#if>
			<#if companyName?exists && companyName!="">
			        COMPANY_NAME = :companyName
			</#if>
			WHERE  ID = :id
		]]>
	</sql>	
</sqlMap>